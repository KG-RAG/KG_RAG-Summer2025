1. 执行testb %al,%al后,若ZF=0,SF=1,则说明AL的值（）
A. 不能确定
B. 等于0
C. 大于0
D. 小于0

2.执行testb %al, %al后,若ZF=1,则说明AL的值( )
A. 不能确定
B. 等于0
C. 大于0
D. 小于0

3.假设short型变量x被编译器分配在寄存器AX中，R[ax] =1F82H，则以下汇编代码段执行后变量x的机器数和真值分别是多少？请写出每条指令执行后的寄存器值AX的值用16进制表示。(结果字母大写）
    movw    %ax, %dx     [填空1] H
    shlw     $2, %ax          [填空2] H
    addw    %dx, %ax        [填空3] H
    shrw    $1, %ax          [填空4] H
